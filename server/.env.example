# Sync server
PORT=4000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=joycecakes
DB_PASSWORD=sua_senha_aqui
DB_NAME=joycecakes_db

# Optional (alternative to DB_* vars)
# DATABASE_URL=postgresql://joycecakes:sua_senha_aqui@localhost:5432/joycecakes_db

# Optional auth key (prod)
API_SECRET_KEY=sua_chave_secreta_aqui

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
