# Pasta `types/` – Tipos TypeScript

Aqui ficam as **interfaces e tipos** do domínio do app (Order, Product, Supply, etc.).

Arquivo principal: **`index.ts`**. Centralize aqui todos os tipos compartilhados.
