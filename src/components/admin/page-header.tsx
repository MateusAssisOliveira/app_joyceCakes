'use client';
// Este arquivo foi substituído por `dynamic-header.tsx` e não é mais usado.
// Pode ser removido com segurança em uma próxima limpeza.
import React from 'react';

export default function PageHeader() {
  return null;
}
